---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <h2>TP18 : Web Components personnalisés</h2>

  <section class="readme">
    <p>Ce TP consiste à créer deux Web Components :</p>
    <ul>
      <li><code>&lt;date-time&gt;</code> : affiche la date et l’heure actuelles, mises à jour en temps réel.</li>
      <li><code>&lt;greet-custom&gt;</code> : affiche une salutation dynamique selon l’heure du jour.</li>
    </ul>
  </section>

  <div class="code-tabs">
    <button class="active-tab" data-tab="ts">app.ts</button>
    <button data-tab="astro">index.astro</button>
  </div>

  <pre id="ts" class="code-block active"><code class="language-ts"></code></pre>
  <pre id="astro" class="code-block"><code class="language-html"></code></pre>

  <div style="margin-top: 1.5rem;">
    <button id="launch-tp18">Lancer le TP</button>
  </div>

  <div class="browser-mockup" style="display: none;" id="browser-container">
    <div class="browser-header">
      <span class="dot red"></span>
      <span class="dot yellow"></span>
      <span class="dot green"></span>
      <span class="url-bar">http://localhost:3000</span>
    </div>
    <iframe id="tp18-frame" class="browser-frame"></iframe>
  </div>

  <script type="module" defer>
    async function loadFile(tab, path) {
      const res = await fetch(path);
      const code = await res.text();
      document.querySelector(`#${tab} code`).textContent = code;
    }

    loadFile('ts', '/data/tp18/app.ts');
    loadFile('astro', '/data/tp18/index.astro');

    const iframe = document.getElementById('tp18-frame');
    const container = document.getElementById('browser-container');

    document.getElementById('launch-tp18').addEventListener('click', () => {
      container.style.display = 'block';
      iframe.src = '/data/tp18/index.html';
    });

    document.querySelectorAll('.code-tabs button').forEach(button => {
      button.addEventListener('click', () => {
        const tab = button.getAttribute('data-tab');
        document.querySelectorAll('.code-block').forEach(el => el.classList.remove('active'));
        document.querySelectorAll('.code-tabs button').forEach(btn => btn.classList.remove('active-tab'));
        document.getElementById(tab).classList.add('active');
        button.classList.add('active-tab');
      });
    });
  </script>

  <style>
    .code-tabs {
      margin-top: 2rem;
    }
    .code-tabs button {
      margin-right: 0.5rem;
      padding: 0.5rem 1rem;
      cursor: pointer;
    }
    .active-tab {
      background-color: #4d7298;
      color: white;
      border: none;
      border-radius: 0.5rem;
    }
    .code-block {
      display: none;
      background: #f4f4f4;
      color: #333;
      padding: 1rem;
      overflow-x: auto;
      border-radius: 0.5rem;
      margin-top: 1rem;
      font-family: monospace;
      white-space: pre;
    }
    .code-block.active {
      display: block;
    }
    .browser-mockup {
      border: 1px solid #ccc;
      border-radius: 8px;
      overflow: hidden;
      margin-top: 2rem;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .browser-header {
      background: #eee;
      padding: 0.5rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .browser-header .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      display: inline-block;
    }
    .browser-header .red { background: #ff5f56; }
    .browser-header .yellow { background: #ffbd2e; }
    .browser-header .green { background: #27c93f; }
    .url-bar {
      background: white;
      border: 1px solid #ccc;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      font-size: 0.85rem;
      flex-grow: 1;
    }
    .browser-frame {
      width: 100%;
      height: 600px;
      border: none;
    }
  </style>
</Layout>
